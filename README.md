# Smart Calendar

A deterministic study calendar for students. It reads assignments and due dates from
Canvas (read-only) and suggests, using plain rules, when to work on them — without
guessing or inventing anything Canvas didn't say.

**Status:** early. We build one phase at a time.

## Start here
- **[docs/DESIGN.md](docs/DESIGN.md)** — the full plan: what it does, the data model,
  the deterministic scheduler, and the phases.
- **[CLAUDE.md](CLAUDE.md)** — the project's hard rules and code standards.

Setup instructions will land here once Phase 1 (the app skeleton) exists.
